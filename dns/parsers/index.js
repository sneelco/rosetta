var request = require('./request'),
  header = require('./header'),
  questions = require('./questions'),
  records = require('./records');

module.exports = {
  request: request,
  header: header,
  questions: questions,
  records: records
};
